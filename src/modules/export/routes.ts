/** Settings route for S11 data portability (TechSpec §95, ADR-014). */
export const EXPORT_SETTINGS_ROUTE = "/settings/data" as const;

/** Label for the Configurações navigation item (T10 wires AuthenticatedShell). */
export const EXPORT_SETTINGS_NAV_LABEL = "Dados" as const;
